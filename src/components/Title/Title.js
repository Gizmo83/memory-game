import React from "react";
import "./Title.css";
import logo from "./logo.png"

const Title = props => (
        <div id="logo">
            <img src={logo} alt={logo}/>
        </div>
)

export default Title;
